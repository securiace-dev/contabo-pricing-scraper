node_modules/
.DS_Store

# Scraped output — regenerated by running the scraper
data/output/

# Rust build artifacts
target/

# Local scratch files
tmp/
*.log

# PHP addon: composer deps + phpunit cache are dev-only. The addon ships with a
# stub autoloader, so prod runs without vendor/ (it's excluded from deploys too).
vendor/
.phpunit.cache/
composer.phar

# Proxy credential — never commit
deploy/proxy.env

# Tool state directories
.claude-flow/
.kilo/
.swarm/
ruvector.db
ruvector.db-*
.claude/memory.db
.claude/memory.db-*
.claude/schema.sql
.claude/state.json

# graphify knowledge-graph output — regenerated by /graphify
graphify-out/
