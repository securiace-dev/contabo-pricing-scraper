# Implementation Plans — WHMCS deployment tooling (dev & prod)

Generated by the improve skill on 2026-06-20 against commit `b31e458`.
Focus: hardening and ergonomics of the deploy/dev tooling under `scripts/`.

Execute in the order below. Plans touch **disjoint file sets** and can run in
parallel. Each executor: read the plan fully, honor its STOP conditions.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Harden the predeploy gate (preflight + honest 7.4 lint + fix runbook refs) | P1 | S | — | DONE (reviewed; in worktree branch `worktree-agent-a98cb699684829374`, commit `79b6875`) |
| 002  | Add dry-run, confirmation & preflights to deploy.sh / local-whmcs.sh | P2 | S | — | DONE (reviewed; in worktree branch `worktree-agent-a29adb7a4be4de892`, commit `8f61692`) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (rationale)

## Dependency notes

- 001 and 002 are independent — `predeploy-check.sh`/`CLAUDE.md` (001) vs
  `deploy.sh`/`local-whmcs.sh` (002) do not overlap. Safe to run concurrently.

## Findings → plans mapping

- Finding #1 (gate reports PASS on 7.4 lint without verifying 7.4) → 001
- Finding #2 (gate hard-FAILs opaquely when prerequisites absent) → 001
- Finding #3 (broken `docs/DEPLOY_RUNBOOK.md` references) → 001
- Finding #4 (no dry-run/confirmation before prod push) → 002
- Finding #5 (no docker/platform/SSH preflight) → 002

## Findings considered and rejected

- `deploy.sh` uses `set -uo pipefail` without `-e`: by design — every command
  has an explicit `|| die`. Not a finding.
- Hardcoded prod IP / root SSH in `deploy.sh`: operational reality (single prod
  box); env-var-ifying it is low value. Not worth doing.
- Auto-deploy CI for WHMCS modules: explicitly rejected in the runbook
  (§scope rule, "intentionally no CI/CD that deploys WHMCS modules"). Settled.
